const props = {
  postProcess: {
    enabled: false,
  },
  debug: {
    postProcess: {
      enabled: false,
    },
    webglHelper: false,
    disableWebgl: false,
  },
  rotationSpeed: 0.01,
};

export default props;
